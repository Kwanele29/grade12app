spring:
  datasource:
    url: jdbc:mysql://localhost:3306/grade12_exam_db?useSSL=false&allowPublicKeyRetrieval=true
    username: root
    password: "Boitumelo@19"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080